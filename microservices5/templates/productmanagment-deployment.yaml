{{- if .Values.productmanagment.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.productmanagment.service.name }}
  namespace: {{ include "microservices5.namespace" . }}
  labels:
    {{- include "microservices5.labels" . | nindent 4 }}
    app: productmanagment
spec:
  replicas: {{ .Values.productmanagment.replicas }}
  selector:
    matchLabels:
      app: productmanagment
  template:
    metadata:
      labels:
        app: productmanagment
    spec:
      initContainers:
        - name: wait-for-configserver
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - |
              until curl -fsS http://{{ .Values.configserver.service.name }}:{{ .Values.configserver.service.port }}/actuator/health | grep -q UP; do
                echo "Waiting for configserver..."; sleep 5;
              done
        {{- if .Values.postgresql.enabled }}
        - name: wait-for-db
          image: postgres:16
          command:
            - sh
            - -c
            - |
              until pg_isready -h db -U postgres; do
                echo "Waiting for db..."; sleep 5;
              done
        {{- end }}
      containers:
        - name: productmanagment
          image: {{ .Values.productmanagment.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.productmanagment.service.port }}
          env:
            - name: SPRING_CONFIG_IMPORT
              value: {{ .Values.productmanagment.configImport | quote }}
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.productmanagment.profilesActive | quote }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.productmanagment.service.port }}
            initialDelaySeconds: {{ .Values.productmanagment.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.productmanagment.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.productmanagment.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.productmanagment.readinessProbe.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.productmanagment.service.port }}
            initialDelaySeconds: {{ .Values.productmanagment.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.productmanagment.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.productmanagment.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.productmanagment.livenessProbe.failureThreshold }}
{{- end }}
