{{- if .Values.ordermanagment.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ordermanagment.service.name }}
  namespace: {{ include "microservices5.namespace" . }}
  labels:
    {{- include "microservices5.labels" . | nindent 4 }}
    app: ordermanagment
spec:
  replicas: {{ .Values.ordermanagment.replicas }}
  selector:
    matchLabels:
      app: ordermanagment
  template:
    metadata:
      labels:
        app: ordermanagment
    spec:
      initContainers:
        - name: wait-for-configserver
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - |
              until curl -fsS http://{{ .Values.configserver.service.name }}:{{ .Values.configserver.service.port }}/actuator/health | grep -q UP; do
                echo "Waiting for configserver..."; sleep 5;
              done
        {{- if .Values.postgresql.enabled }}
        - name: wait-for-db
          image: postgres:16
          command:
            - sh
            - -c
            - |
              until pg_isready -h db -U postgres; do
                echo "Waiting for db..."; sleep 5;
              done
        {{- end }}
      containers:
        - name: ordermanagment
          image: {{ .Values.ordermanagment.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.ordermanagment.service.port }}
          env:
            - name: SPRING_CONFIG_IMPORT
              value: {{ .Values.ordermanagment.configImport | quote }}
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.ordermanagment.profilesActive | quote }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.ordermanagment.service.port }}
            initialDelaySeconds: {{ .Values.ordermanagment.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.ordermanagment.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.ordermanagment.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.ordermanagment.readinessProbe.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.ordermanagment.service.port }}
            initialDelaySeconds: {{ .Values.ordermanagment.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.ordermanagment.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.ordermanagment.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.ordermanagment.livenessProbe.failureThreshold }}
{{- end }}
