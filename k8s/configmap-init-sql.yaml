apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
  namespace: ecommerce
data:
  init.sql: |
    CREATE DATABASE clients_db;
    CREATE DATABASE orders_db;
    CREATE DATABASE products_db;
    CREATE DATABASE productincart_db;
