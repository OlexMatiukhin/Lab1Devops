apiVersion: v1
kind: Service
metadata:
  name: productincartmanagment
  namespace: ecommerce
spec:
  selector:
    app: productincartmanagment
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
